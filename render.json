{
  "scripts": {
    "preinstall": "echo 'Installing backend dependencies for Render...'",
    "postinstall": "echo 'Backend ready for deployment!'",
    "health-check": "curl -f http://localhost:$PORT/health || exit 1"
  }
}